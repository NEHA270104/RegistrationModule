# Quality Assurance & Test Cases Suite (QA_TEST_CASES.md)

**Document Version:** 2.0.0  
**Test Suite Target:** EventReg Platform Production Release  
**Execution Type:** Manual Functional QA & Automated E2E Regression  
**Coverage Scope:** Netlify Edge Routing, Onboarding Provisioning, Subscription Gating, Theming, AI Flyer Studio, Public Registration Funnel, and Real-Time Supabase Synchronization.  

---

## 1. Test Execution Summary Matrix

| Module Code | Module Name | Total Test Cases | High / Critical | Medium | Low | Automation Target |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **ROUT** | Netlify Root Routing & CDN Redirects | 5 | 5 | 0 | 0 | Playwright E2E |
| **PROV** | Pre-Payment Provisioning & Password Auth | 8 | 7 | 1 | 0 | Jest / Supertest |
| **SUB** | Subscription Plans & Feature Gating | 8 | 6 | 2 | 0 | Unit / Integration |
| **PAY** | Razorpay Payment & Order Verification | 7 | 7 | 0 | 0 | Cypress / Mock API |
| **THEME** | Dashboard Theme Customization & Sync | 6 | 3 | 3 | 0 | Visual Regression |
| **FLYER** | AI Flyer Studio & Progressive Quiz Engine | 7 | 5 | 2 | 0 | Playwright E2E |
| **REG** | Hosted Public Event Registration (`/register`) | 8 | 7 | 1 | 0 | Playwright E2E |
| **SYNC** | Real-Time Supabase Sync & Attendee Hub | 7 | 6 | 1 | 0 | WebSocket Mock |
| **SEC** | Security, RLS Isolation & Role Guards | 6 | 6 | 0 | 0 | Security Pentest |
| **E2E** | Full Lifecyle End-to-End Scenarios | 4 | 4 | 0 | 0 | CI/CD Nightly |
| **TOTAL** | **Comprehensive Platform Test Suite** | **66** | **56** | **10** | **0** | **100% Coverage** |

---

## 2. Detailed Tabular Test Suites

### Module 1: Netlify Root Routing & CDN Redirects (ROUT)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-ROUT-001** | **Root Domain Rewrite Verification**<br>1. Open clean incognito browser.<br>2. Navigate to `https://<domain>/`.<br>3. Inspect Network tab and URL bar. | URL: `https://<domain>/`<br>Method: `GET` | HTTP status `200 OK`. URL in browser bar remains `/`. Page content served is the SaaS Landing Page (`EventReg Platform - Launch Event Registrations in Minutes`). | **Pass:** Landing page loads seamlessly at root URL with 200 OK.<br>**Fail:** 404 error, redirect loop, or old summit preview page served. |
| **TC-ROUT-002** | **Direct Root Index Fallback Redirection**<br>1. Explicitly navigate to `https://<domain>/index.html`.<br>2. Observe HTTP and DOM execution. | URL: `https://<domain>/index.html` | Client-side script executes `window.location.replace('./landing/index.html')` and `<meta http-equiv="refresh">` triggers. Browser instantly transitions to `/landing/index.html`. | **Pass:** Instant redirection without user delay or blank screen.<br>**Fail:** Renders old MSME summit preview page or throws 404. |
| **TC-ROUT-003** | **SaaS Landing Page Direct Access**<br>1. Navigate directly to `https://<domain>/landing/index.html`.<br>2. Verify all CSS, JS, and image assets load. | URL: `/landing/index.html`<br>Assets: `/landing/css/landing.css`, `/landing/js/landing.js` | All assets return HTTP `200 OK` (0 broken images, 0 console errors). Responsive navigation bar, pricing toggle, and CTAs function properly. | **Pass:** All assets load with 200 status code.<br>**Fail:** Any asset 404 or broken styling. |
| **TC-ROUT-004** | **Landing Page CTA Navigation Links**<br>1. Click "Get Started Free" / "Create Your Event Free".<br>2. Click "Login" / "Login to Dashboard". | CTAs on Landing Page | "Get Started Free" navigates to `/onboarding`. "Login" navigates to `/dashboard`. | **Pass:** Correct destination routing for all action buttons.<br>**Fail:** Broken links or incorrect paths. |
| **TC-ROUT-005** | **Edge Rewrite Configuration Resilience**<br>1. Inspect repository `netlify.toml` and `frontend/_redirects`.<br>2. Verify `base = "frontend"` and `publish = "."`. | File: `netlify.toml`, `_redirects` | Configuration contains valid TOML structure and correct rewrite rule: `[[redirects]] from = "/" to = "/landing/index.html" status = 200`. | **Pass:** Valid syntax adhering to Netlify specification.<br>**Fail:** Syntax errors or missing rewrite block. |

---

### Module 2: Pre-Payment Account Provisioning & Password Registration (PROV)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PROV-001** | **Live Slug Availability Validation**<br>1. Navigate to `/onboarding`.<br>2. Enter company name.<br>3. Type candidate slug in the custom URL input. | Slug: `tech-summit-2026`<br>Slug: `already-taken-slug` | Real-time debounced API call checks availability. Green checkmark for available slug; red warning alert if slug already exists. | **Pass:** Real-time feedback correctly reflects database state.<br>**Fail:** False positive or unhandled collision. |
| **TC-PROV-002** | **Step 1 Pre-Provisioning Form Validation**<br>1. Leave email or password empty.<br>2. Enter weak password (< 6 chars).<br>3. Click "Continue to Plan Selection". | Invalid inputs | Client highlights required fields. Form submission is blocked until valid inputs and secure password are provided. | **Pass:** Client validation prevents submission of invalid data.<br>**Fail:** Form submits with empty or invalid credentials. |
| **TC-PROV-003** | **Pre-Payment Staged User & Tenant Creation**<br>1. Fill valid Step 1 data (Company, Slug, Email, Password).<br>2. Click "Continue".<br>3. Check API response and database state. | Payload: `{ company_name: "Apex Labs", slug: "apex-summit", email: "admin@apex.io", password: "Password@123" }` | Endpoint `POST /api/auth/pre-provision` responds `201 Created`. Database has tenant with `status = 'pending_payment'`. User created in Supabase Auth. Step 2 loads. | **Pass:** Staged record created without full dashboard access.<br>**Fail:** 500 error or user granted premature active access. |
| **TC-PROV-004** | **Unpaid Session Guard & Resumption**<br>1. Complete Step 1.<br>2. Close browser tab on Step 2 (payment).<br>3. Re-open `/onboarding`. | Stored `onboarding_token` / `staged_session` | Wizard executes `checkExistingSession()`, detects unpaid registered tenant, and skips directly to Step 2 with stored plan choices. | **Pass:** Smoothly resumes at Step 2 without re-entering Step 1.<br>**Fail:** Restarts at Step 1 or throws authorization exception. |
| **TC-PROV-005** | **Paid Session Auto-Redirect**<br>1. Authenticate as paid, active tenant.<br>2. Attempt to navigate directly to `/onboarding`. | Valid active `dashboard_access_token` | Session guard detects `status = 'active'` and immediately redirects browser to `/dashboard/[slug]`. | **Pass:** Prevents duplicate onboarding for active tenants.<br>**Fail:** Renders onboarding form for already-paid user. |
| **TC-PROV-006** | **Password Complexity & Hashing Verification**<br>1. Register with secure password.<br>2. Verify database records in `auth.users`. | Password: `SummitSecure#2026!` | Password hash stored using Bcrypt / Argon2. Plaintext password never appears in PostgreSQL tables or debug logs. | **Pass:** Airtight credential hashing.<br>**Fail:** Any plaintext password exposure. |
| **TC-PROV-007** | **Step 1 Idempotent Re-Submission**<br>1. Submit Step 1.<br>2. Click "Back" from Step 2 to Step 1.<br>3. Modify company phone number and re-submit. | Modified payload with same `email` and `slug` | Updates existing staged tenant row instead of creating duplicate database records or throwing SQL primary key collision error. | **Pass:** HTTP 200/201 with updated staged tenant properties.<br>**Fail:** 500 duplicate key error. |
| **TC-PROV-008** | **Password Reset & Account Recovery Flow**<br>1. Navigate to `/recover.html`.<br>2. Enter registered tenant email.<br>3. Follow reset link to `/reset-password.html`. | Email: `admin@apex.io` | Recovery email sent. Password successfully updated in Supabase Auth. User can log in with new password. | **Pass:** Complete recovery loop functions end-to-end.<br>**Fail:** Token expired error or password update failure. |

---

### Module 3: Subscription Plans, Pricing & Feature Gating (SUB)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-SUB-001** | **Pricing Plan Display & Yearly Discount Toggle**<br>1. Navigate to Pricing section on Landing Page or Onboarding Step 2.<br>2. Toggle between "Monthly" and "Yearly". | Toggle: Monthly / Yearly | **Basic:** ₹1/mo or ₹10/yr.<br>**Standard:** ₹5/mo or ₹50/yr.<br>**Premium:** ₹10/mo or ₹100/yr.<br>Yearly displays "Save 17%" badge. Amounts update dynamically. | **Pass:** Prices and currency symbols match exact specifications.<br>**Fail:** Incorrect pricing calculations or broken toggle state. |
| **TC-SUB-002** | **Basic Plan (₹1/mo) Event Creation Quota Enforcement**<br>1. Log in as Basic plan tenant.<br>2. Create 3 events in Events Hub.<br>3. Attempt to create a 4th event. | Tenant Plan: `basic`<br>Active Events: `3` | 4th event creation is blocked on both client UI and server API (`HTTP 403`). Upgrade modal is displayed prompting upgrade to Standard (10 events) or Premium (50 events). | **Pass:** Strict ceiling at 3 events enforced.<br>**Fail:** User allowed to create > 3 events. |
| **TC-SUB-003** | **Standard Plan (₹5/mo) Feature Verification**<br>1. Log in as Standard plan tenant.<br>2. Create up to 10 events.<br>3. Access Advanced Analytics and export CSV.<br>4. Attempt to open AI Flyer Studio. | Tenant Plan: `standard` | Events 1-10 succeed. CSV export downloads valid attendee data. AI Flyer Studio displays locked state prompting upgrade to Premium. | **Pass:** Analytics/CSV unlocked; AI Flyer Studio locked.<br>**Fail:** CSV export blocked or AI Flyer unlocked. |
| **TC-SUB-004** | **Premium Plan (₹10/mo) Full Feature Unlocking**<br>1. Log in as Premium plan tenant.<br>2. Create up to 50 events.<br>3. Access AI Flyer Studio.<br>4. Generate multi-format flyers and access custom branding. | Tenant Plan: `premium` | All 50 event slots permitted. AI Flyer Studio 5-step wizard fully operational. Advanced Analytics, CSV export, and VIP features accessible. | **Pass:** Zero feature gating restrictions for Premium.<br>**Fail:** Any Premium feature locked or gated. |
| **TC-SUB-005** | **Server-Side API Plan Gating (`planGuard.ts`)**<br>1. Use curl/Postman with Basic tier JWT.<br>2. Send `POST /api/t/:slug/flyers/generate`. | Header: `Authorization: Bearer <basic_token>`<br>Endpoint: `/api/t/:slug/flyers/generate` | Server returns HTTP `403 Forbidden` with `{ success: false, code: "PLAN_FEATURE_LOCKED", message: "Upgrade to Premium to access AI Flyer Studio" }`. | **Pass:** Backend rejects unauthorized feature execution.<br>**Fail:** Backend executes request without tier verification. |
| **TC-SUB-006** | **Plan Upgrade Mid-Cycle Flow**<br>1. Basic tenant clicks "Upgrade to Premium" in Dashboard.<br>2. Complete ₹10 checkout. | Plan: `basic` -> `premium` | Subscription updated in Supabase. JWT claims refreshed. AI Flyer Studio and higher event quotas instantly become active without relogin. | **Pass:** Immediate real-time feature unlock upon upgrade.<br>**Fail:** User remains restricted until manual logout/login. |
| **TC-SUB-007** | **Attendee Limit Overflow Alert**<br>1. Basic tier tenant approaches 500 attendee limit.<br>2. Register 501st attendee on public form. | Attendees Count: `500` -> `501` | System logs `limit_exceeded` notification in `admin_notifications`. Admin alerts widget warns organizer to upgrade. | **Pass:** Proper telemetry alert logged without data loss.<br>**Fail:** Unhandled fatal exception on public form. |
| **TC-SUB-008** | **Admin Override Bypass Verification**<br>1. Authenticate as Super Admin.<br>2. Add event/guest for Basic tenant beyond quota with `admin_override = true`. | Header: `x-api-key` or `role: super_admin`<br>Payload: `{ admin_override: true }` | Action succeeds. Inserted record in DB has `manual_admin_entry = true` for auditing. | **Pass:** Admin override functions with clear audit flag.<br>**Fail:** Admin blocked or audit flag missing. |

---

### Module 4: Payment Gateway & Razorpay Order Verification (PAY)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PAY-001** | **Razorpay Order Creation Integrity**<br>1. Select Standard plan (Monthly ₹5 = 500 paise).<br>2. Trigger checkout. | Payload: `{ plan: "standard", cycle: "monthly" }` | Server calls Razorpay Orders API. Returns `order_id` (e.g. `order_K9xyz...`), exact amount in paise (`500`), and active `key_id`. | **Pass:** Valid Razorpay order created with exact paise amount.<br>**Fail:** Amount discrepancy or failed order creation. |
| **TC-PAY-002** | **Razorpay Checkout Modal Launch**<br>1. Click "Pay & Launch Workspace".<br>2. Verify Razorpay modal overlay. | Client Razorpay SDK | Razorpay checkout popup opens displaying company name, logo, pre-filled organizer email/phone, and exact INR amount. | **Pass:** Modal renders seamlessly with pre-filled metadata.<br>**Fail:** Modal fails to open or throws JavaScript error. |
| **TC-PAY-003** | **Cryptographic Signature Verification**<br>1. Complete payment in Razorpay test mode.<br>2. Client sends verification payload. | Payload: `{ razorpay_order_id, razorpay_payment_id, razorpay_signature }` | Backend computes `crypto.createHmac('sha256', secret).update(order_id + '|' + payment_id).digest('hex')`. Signatures match -> returns HTTP 200. | **Pass:** Cryptographic verification succeeds; tenant activated.<br>**Fail:** Signature mismatch or bypass allowed. |
| **TC-PAY-004** | **Tampered Signature Rejection**<br>1. Intercept verification request and alter `razorpay_signature`.<br>2. Dispatch modified payload to `/api/payments/verify-onboarding`. | Tampered `razorpay_signature` | Backend rejects with HTTP `400 Bad Request` or `401 Unauthorized` (`INVALID_SIGNATURE`). Database tenant remains `pending_payment`. | **Pass:** Tampered payment rejected; tenant not activated.<br>**Fail:** Security breach: tenant activated on invalid signature. |
| **TC-PAY-005** | **Laser UPI QR Payment Simulation**<br>1. In test environment, select UPI QR Simulator.<br>2. Observe scanner UI animation.<br>3. Simulate successful scan. | UI Modal: `showQrPaymentModal` | Laser scanning effect renders smoothly. Upon simulated confirmation, triggers verification endpoint and advances onboarding. | **Pass:** High-tech simulation transitions state accurately.<br>**Fail:** Animation glitches or state hangs. |
| **TC-PAY-006** | **Payment Cancellation & Retry Flow**<br>1. Open Razorpay modal.<br>2. Dismiss modal without paying.<br>3. Click "Retry Payment". | User cancels checkout modal | Toast alert informs user "Payment was cancelled". Form remains on Step 2. Clicking Retry generates fresh order without breaking state. | **Pass:** Clean recovery with zero orphaned corrupted state.<br>**Fail:** UI locks up or requires full page reload. |
| **TC-PAY-007** | **Webhook Asynchronous State Reconciliation**<br>1. Simulate Razorpay `payment.captured` webhook directly to `/api/payments/webhook`. | Webhook payload signed with `X-Razorpay-Signature` | Server verifies webhook signature, locates corresponding tenant/subscription, and ensures `status = 'active'`. | **Pass:** Idempotent background reconciliation succeeds.<br>**Fail:** Webhook signature failure or unhandled event. |

---

### Module 5: Unified Dashboard Theme Customization & Persistence (THEME)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-THEME-001** | **Default Dark High-Tech Theme Rendering**<br>1. Log in to Dashboard for the first time.<br>2. Inspect `<html>` attributes and computed styles. | Clean session (no saved theme) | `document.documentElement` defaults to Dark High-Tech. Background is deep obsidian (`#090d16`), cards have subtle cyan borders (`rgba(56, 189, 248, 0.2)`). | **Pass:** Clean dark high-tech presentation without white flash.<br>**Fail:** Broken CSS variables or white unstyled background. |
| **TC-THEME-002** | **Switch to Light Clean Theme**<br>1. Open header theme toggle pill or Settings page.<br>2. Select "Light Clean". | Action: Select `light` | `[data-theme="light"]` attribute applied to `<html>`. Background changes to `#f8fafc`, typography becomes deep indigo `#3730a3`, inputs adopt light backgrounds. | **Pass:** Instant smooth transition with legible contrast.<br>**Fail:** Illegible text contrast or missing light overrides. |
| **TC-THEME-003** | **Switch to Vibrant Neon Theme**<br>1. Select "Vibrant Neon".<br>2. Observe sidebar and accent cards. | Action: Select `neon` | `[data-theme="neon"]` applied. Background becomes `#050811`, primary accent switches to neon pink `#f0abfc`, sidebar renders scanline texture overlay. | **Pass:** Luminous cyberpunk aesthetics rendered properly.<br>**Fail:** Accent colors fail to update. |
| **TC-THEME-004** | **LocalStorage Theme Persistence Across Reloads**<br>1. Set theme to "Vibrant Neon".<br>2. Hard reload browser (`Ctrl + F5`). | `localStorage.getItem('dashboard_theme') === 'neon'` | Page reloads directly in Vibrant Neon theme. Zero Flash of Unstyled Content (FOUC) or momentary white flash during load. | **Pass:** Theme persists synchronously before DOM paint.<br>**Fail:** White flash or resets to default theme. |
| **TC-THEME-005** | **Cloud Profile Theme Sync to Supabase**<br>1. Change theme in Settings page.<br>2. Check network requests and database `tenants.settings`. | Payload: `PATCH /api/t/:slug/settings { theme: 'light' }` | API returns HTTP 200. Supabase tenant record updated. Logging in from a different browser loads the saved cloud theme. | **Pass:** Cross-device persistence verified.<br>**Fail:** Setting not saved to remote database. |
| **TC-THEME-006** | **Interactive Settings Theme Card Selector**<br>1. Navigate to `/dashboard/[slug]/settings`.<br>2. Click theme cards (Dark, Light, Neon). | UI: `.theme-card` grid | Active card receives glowing selection ring and checkmark badge. DOM theme switches synchronously with smooth 200ms ease. | **Pass:** Flawless visual feedback and immediate state change.<br>**Fail:** Card click does not trigger theme change. |

---

### Module 6: Automated Flyer-to-Form Studio & Public Event Hosting (FLYER & REG)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-FLYER-001** | **5-Step Progressive Studio Wizard Navigation**<br>1. Open AI Flyer Studio in Dashboard.<br>2. Advance through Steps 1 to 5 using "Next" and dot indicators. | Step 1 -> Step 2 -> Step 3 -> Step 4 -> Step 5 | Progress dots animate smoothly. Previous step data is retained. Step transitions require required fields to be valid. | **Pass:** Fluid step progression and backward navigation.<br>**Fail:** Step navigation breaks or drops form data. |
| **TC-FLYER-002** | **Reactive Live Canvas Preview**<br>1. In Step 1, type event title and change accent color.<br>2. In Step 3, add 2 guest speakers with avatar URLs. | Title: `Global AI Summit 2026`<br>Accent: `#00f5ff` | Live canvas preview panel on the right updates instantly in real time without lag or page refresh. | **Pass:** Immediate sub-50ms reactive preview rendering.<br>**Fail:** Preview fails to update or renders broken HTML. |
| **TC-FLYER-003** | **Flyer Export to High-Resolution PNG & PDF**<br>1. In Step 5, click "Export PNG".<br>2. Click "Export PDF". | Canvas capture engine | Generates crisp, downloadable high-res `.png` and multi-page `.pdf` assets matching canvas dimensions. | **Pass:** Files download cleanly with all fonts and graphics intact.<br>**Fail:** Blurry export, missing text, or CORS canvas taint. |
| **TC-FLYER-004** | **Step 5 JSON Schema Generation & Form Publishing**<br>1. Click "Publish Event to Live Form".<br>2. Verify API payload and database record. | Endpoint: `POST /api/t/:slug/events` | Event record created in Supabase with complete agenda, speakers array, and ticket tiers. Public slug generated. | **Pass:** Event published with matching JSON configuration.<br>**Fail:** Missing relational fields or database error. |
| **TC-REG-001** | **Public Registration Page Resolution (`/register/[event-slug]`)**<br>1. Navigate to `https://<domain>/register/global-ai-summit-2026`.<br>2. Verify page rendering. | URL: `/register/global-ai-summit-2026` | Page resolves via Netlify rewrite. Fetches `/api/public/events/global-ai-summit-2026`. Title, banner, speakers, and tiers display accurately. | **Pass:** Public registration page hydrates completely.<br>**Fail:** 404 Not Found or empty unpopulated template. |
| **TC-REG-002** | **Interactive Ticket Tier Selection on Public Form**<br>1. Click "VIP Pass" (₹999).<br>2. Click "General Access" (₹499). | UI: `.pass-option` cards | Selected card highlights with accent border. Hidden input `#selected-pass-type` updates. Total payable updates dynamically. | **Pass:** Real-time pass selection and price calculation.<br>**Fail:** Pass selection unresponsive. |
| **TC-REG-003** | **Attendee Registration Form Submission**<br>1. Fill Attendee Name, Email, Phone.<br>2. Select Pass Type.<br>3. Click "Complete Registration". | Payload: `{ name: "John Doe", email: "john@acme.com", phone: "+919876543210", pass_type: "vip" }` | Form validates phone/email. Launches Razorpay modal for ticket fee. On payment, displays Confirmation Screen with Registration ID. | **Pass:** End-to-end attendee registration and ticket issuing.<br>**Fail:** Validation failure or payment loop error. |
| **TC-REG-004** | **Free Ticket Instant Confirmation**<br>1. Register for a ₹0 Free Pass tier. | Pass Price: `₹0` | Bypasses Razorpay gateway. Instantly inserts confirmed attendee in Supabase and displays success screen with QR ticket. | **Pass:** Free passes process without gateway invocation.<br>**Fail:** Erroneous gateway launch for zero-amount tickets. |

---

### Module 7: Real-Time Attendee Database & Events Hub Supabase Sync (SYNC)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-SYNC-001** | **Real-Time Supabase WebSocket Telemetry**<br>1. Open Organizer Dashboard in Browser A.<br>2. Register a new attendee on the public registration page in Browser B. | Supabase Event: `INSERT on public.attendees` | Browser A immediately adds the new attendee row to the top of the table and increments "Total Registered" counter without page refresh. | **Pass:** Sub-second live WebSocket synchronization.<br>**Fail:** Requires manual page reload to view new attendee. |
| **TC-SYNC-002** | **Live KPI Counter Increments & Animations**<br>1. Observe KPI counters ("Seats Left", "Revenue Today") during attendee registration. | Attendee Ticket: `VIP (₹999)` | "Seats Left" decrements by 1. "Revenue Today" increments by ₹999. Counter briefly flashes with accent pulse animation. | **Pass:** Accurate mathematical tally with visual cue.<br>**Fail:** Inaccurate count or missing update. |
| **TC-SYNC-003** | **Attendee Table Search & Column Filtering**<br>1. Type attendee name or email in search bar.<br>2. Filter by Pass Type ("VIP", "General"). | Search: `john@acme.com`<br>Filter: `VIP` | Table filters instantly client-side. Matches highlighted. Displays "No attendees found" empty state if query yields zero matches. | **Pass:** Fast, accurate multi-parameter filtering.<br>**Fail:** Filter returns incorrect rows or crashes. |
| **TC-SYNC-004** | **Attendee Database Pagination**<br>1. Load event with > 50 attendees (`PAGE_SIZE = 20`).<br>2. Click Next Page / Page 2. | Page: `2` (Records 21-40) | Table displays records 21-40. Pagination indicator shows "Showing 21-40 of 55". Controls disable at start/end bounds. | **Pass:** Correct slice rendering and boundary guards.<br>**Fail:** Records skipped or pagination buttons fail. |
| **TC-SYNC-005** | **Attendee CSV & Excel Export**<br>1. In Standard or Premium tier account, click "Export to CSV".<br>2. Open downloaded `.csv` file. | Export Action | File `attendees_<event_slug>_<timestamp>.csv` downloads. Contains formatted columns: Name, Email, Phone, Pass Type, Amount, Payment ID, Date. | **Pass:** Valid CSV file with correct UTF-8 headers and data.<br>**Fail:** Empty file, corrupted columns, or missing rows. |
| **TC-SYNC-006** | **Events Hub Multi-Event Switcher**<br>1. Organizer with multiple events selects "Tech Summit" then switches to "Design Workshop". | Event Selector Dropdown | Attendee list, metrics, and event settings switch dynamically to the selected event context. | **Pass:** Instant context switch with isolated metrics.<br>**Fail:** Data cross-contamination between events. |
| **TC-SYNC-007** | **Network Disconnection & WebSocket Auto-Reconnect**<br>1. Disconnect network for 10 seconds while on Attendee Hub.<br>2. Reconnect network. | Network toggle: Offline -> Online | Supabase realtime client reconnects automatically. Polling fallback syncs any attendee records registered during downtime. | **Pass:** Resilient automatic reconnection and sync catch-up.<br>**Fail:** Silent failure leaving dashboard in permanent stale state. |

---

### Module 8: Security, RLS Isolation & Multi-Tenancy (SEC)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-SEC-001** | **Cross-Tenant Data Isolation (Row-Level Security)**<br>1. Log in as Tenant A (`tenant_id_1`).<br>2. Query `/api/t/tenant-b-slug/attendees`. | Header: `Authorization: Bearer <tenant_a_token>`<br>Path: `/api/t/tenant-b/attendees` | Server returns HTTP `403 Forbidden` (`CROSS_TENANT_ACCESS_DENIED`). Supabase RLS blocks cross-tenant database reads. | **Pass:** Strict tenant boundary isolation.<br>**Fail:** Tenant A can view Tenant B's data (Critical Severity). |
| **TC-SEC-002** | **Unauthenticated Dashboard Access Blocking**<br>1. Open clean browser without JWT token.<br>2. Attempt to navigate to `/dashboard/my-tenant/overview`. | No `dashboard_access_token` in storage | `DashboardAuth.isAuthenticated()` evaluates `false`. Browser immediately redirects to `/dashboard` login screen. | **Pass:** Protected routes inaccessible without valid JWT.<br>**Fail:** Unauthenticated user views private dashboard. |
| **TC-SEC-003** | **Expired JWT Token Auto-Refresh & Logout**<br>1. Simulate expired JWT token.<br>2. Make an authenticated API request. | Expired Access Token + Valid Refresh Token | Supabase client uses refresh token to obtain fresh access token transparently. If refresh token expired, user is logged out safely. | **Pass:** Seamless refresh or graceful logout redirect.<br>**Fail:** Endless error loop or application crash. |
| **TC-SEC-004** | **Input Sanitization & XSS Prevention**<br>1. In Event Title or Speaker Name, inject `<script>alert('XSS')</script>`.<br>2. Save and view on public registration page. | Payload: `<img src=x onerror=alert(1)>` | Input is escaped (`escapeHTML()`) before DOM insertion. Script does not execute. Displayed as harmless plain text. | **Pass:** Zero script execution; XSS payloads neutralized.<br>**Fail:** Alert popup triggers or script executes. |
| **TC-SEC-005** | **Super-Admin Service Role Isolation**<br>1. Attempt to call Super Admin API (`/api/super-admin/tenants`) with regular tenant JWT. | Regular Tenant JWT | Server rejects with HTTP `403 Forbidden`. Only tokens with `role: 'super_admin'` or valid `x-api-key` are authorized. | **Pass:** Super-admin endpoints strictly isolated.<br>**Fail:** Privilege escalation allowed. |
| **TC-SEC-006** | **SQL Injection & ORM Parameterization**<br>1. Pass `' OR '1'='1` into slug, email, and search parameters. | Query: `?slug=' OR '1'='1` | Parameterized Supabase queries sanitize input. Handled as a literal string. Zero database errors or unintended record returns. | **Pass:** Completely immune to SQL injection.<br>**Fail:** Syntax error or unauthorized records returned. |

---

### Module 9: End-to-End User Journeys (E2E)

| Test Case ID | Test Scenario & Steps | Test Data / Payload | Expected Result | Pass/Fail Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **TC-E2E-001** | **Complete Organizer Onboarding to First Event Launch**<br>1. User lands on `/` (served `/landing/index.html`).<br>2. Clicks "Get Started Free" -> `/onboarding`.<br>3. Enters company identity, custom slug, sets password.<br>4. Selects Premium Plan (₹10/mo) and completes payment.<br>5. Redirects to `/dashboard/[slug]`.<br>6. Launches AI Flyer Studio, completes 5-step quiz, publishes event.<br>7. Public event goes live at `/register/[slug]`. | Complete multi-step journey | Seamless end-to-end execution across 7 distinct steps. Database contains linked tenant, subscription, and event records. | **Pass:** All steps complete without errors or friction.<br>**Fail:** Any step fails, blocks, or corrupts state. |
| **TC-E2E-002** | **Complete Attendee Registration to Real-Time Telemetry Loop**<br>1. Attendee opens `/register/[event-slug]`.<br>2. Selects VIP ticket (₹999), fills form.<br>3. Completes Razorpay checkout.<br>4. Receives Confirmation Screen & QR Pass.<br>5. Organizer viewing Dashboard sees attendee row appear live in WebSocket stream with updated KPI totals. | Full attendee conversion + organizer sync | Dual-sided verification: attendee receives pass; organizer receives instantaneous real-time telemetry update. | **Pass:** Real-time loop closes within 1 second.<br>**Fail:** Attendee payment fails or organizer telemetry delayed. |
| **TC-E2E-003** | **Subscription Plan Upgrade & Feature Unlocking Cycle**<br>1. Onboard on Basic Plan (₹1/mo).<br>2. Observe locked AI Flyer Studio and 3-event limit.<br>3. Click "Upgrade to Premium" in Dashboard.<br>4. Pay ₹10 upgrade fee.<br>5. Verify immediate unlock of AI Flyer Studio, 50 event slots, and CSV export. | Upgrade transition | Subscription tier elevates from Basic to Premium. UI permissions refresh dynamically without requiring logout. | **Pass:** Flawless live upgrade with zero downtime.<br>**Fail:** Permissions fail to unlock after successful payment. |
| **TC-E2E-004** | **Custom Branding & Theme Customization Verification**<br>1. Organizer uploads custom company logo in Settings.<br>2. Sets primary brand color to `#10b981`.<br>3. Selects "Vibrant Neon" theme.<br>4. Verifies Dashboard and Public Registration page reflect custom branding. | Branding & Theme configuration | Dashboard adopts Vibrant Neon theme with `#10b981` brand highlights and logo. Public form reflects organizer logo and accent color. | **Pass:** Full white-label and theme synchronization.<br>**Fail:** Brand assets fail to propagate. |
